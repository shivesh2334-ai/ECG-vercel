# No server-side env vars are required — users paste their own Gemini API key
# into the app UI at runtime, exactly as in the original Streamlit tool.
# This file is kept for future use (e.g. if you later want to pin a default key).
